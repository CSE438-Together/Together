type Post @model @auth(rules: [{allow: public}]) {
  id: ID!
  author: String
  departureTime: String
  source: String
  destination: String
  transportation: String
  description: String
  maxMembers: Int
  title: String
  postTime: String
}
 